Notes:

-The current code does not even directly use pokeapi or any fetching, for that matter.  It just has img src attributes.

-The current app will not support duplicate cards (and I don't see a reason why I should have 'em).

-Future Idea:
-ideas for random other fad things to put in there for fun: ash ketchum, terrako, battle-tested guardian, revali, a squid kid
-and maybe forget the stock stuff fetched from online, and provide ai-generated images of your own original (pokemon) characters!

-Add round count. (maybe connect to localstorage, too)

-Add artwork type switching: official, svg, 3d, animated.
  // return `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/showdown/${pokeId}.gif`;
  // return `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/home/${pokeId}.png`;
  // return `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/dream-world/${pokeId}.svg`;
  // return `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/${pokeId}.png`;
-Then add a toggle to view the backs of the pokemon!
-And shiny!  So it is basically a pokemon artwork viewer that doubles up as a fun little game.
-Maybe as a harder difficulty setting, pokemon are 50/50 shiny.
-Or just a shiny mode.
-Remember to have cool effects!

-and a toggle to turn all pokemon to the right. you can do it with just scaleX(-1)! (and of course have a cool transition with it)
-or maybe mixed? (but nah the lighting is all top-left)

-Maybe a feature where you can make your own list of pokemon!

-Add round count. (maybe connect to localstorage, too)

-Timers and count-ups

-Have a "copy artwork" mode, where you can click on pokemon to copy it to clipboard.
-And you can drag them from the browser and drop them onto your computer (turn pointer-events to normal)

-Add favicon
